#include <tunables/global>
profile power_button flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  
  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),
  
  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,
  
  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,
  
  # Access to options.json and other files within addon
  /data/** rw,
  
  # Start new profile for service
  /usr/bin/python3 cx -> power_button_service,
  
  profile power_button_service flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>
    #include <abstractions/python>
    
    # Receive signals from S6-Overlay
    signal (receive) peer=*_power_button,
    
    # Access to options.json and other files within addon
    /data/** rw,
    
    # Python executable and script
    /usr/bin/python3 r,
    /power_button.py r,
    
    # Python libraries
    /usr/lib/python3*/** mr,
    /usr/local/lib/python3*/** mr,
    
    # Input device access for power button
    /dev/input/ r,
    /dev/input/event* rw,
    /sys/class/input/ r,
    /sys/class/input/** r,
    /sys/devices/** r,
    /proc/bus/input/ r,
    /proc/bus/input/** r,
    
    # Network access for API calls
    network inet stream,
    network inet6 stream,
    network inet dgram,
    network inet6 dgram,
    
    # DNS resolution
    /etc/hosts r,
    /etc/resolv.conf r,
    /etc/nsswitch.conf r,
    
    # SSL/TLS certificates
    /etc/ssl/** r,
    /usr/share/ca-certificates/** r,
    
    # Basic system files
    /etc/passwd r,
    /etc/localtime r,
    /usr/share/zoneinfo/** r,
    
    # Standard devices
    /dev/null rw,
    /dev/zero r,
    /dev/urandom r,
    /dev/stdout w,
    /dev/stderr w,
    /dev/tty rw,
    
    # Process information
    /proc/version r,
    /proc/self/** r,
    /proc/*/stat r,
    
    # Temporary files
    /tmp/** rw,
    
    # Basic shell commands
    /bin/bash rix,
    /bin/echo ix,
  }
}